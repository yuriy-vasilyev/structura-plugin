# Channels routes

Top-level Channels admin page route. Mirrors `client/src/features/ai-engine/routes/`.

Phase 2 lands `Channels.tsx` (two-section page: connected + available cards). Phase 6 adds
the free-tier teaser variant.
